import React from "react";
import { RegisterForm } from "@/components/auth/SignupForm";
const page = () => {
  return (
    <RegisterForm>
      
      </RegisterForm>
    );
};

export default page;
